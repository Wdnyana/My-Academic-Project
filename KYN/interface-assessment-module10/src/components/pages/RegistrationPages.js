import React from "react";

import LayoutForm from "../layouts/LayoutForm";
import FormRegistration from "../content/FormRegistration";

const RegistrationPages = () => {
  return (
    <LayoutForm>
      <FormRegistration />
    </LayoutForm>
  );
};

export default RegistrationPages;
