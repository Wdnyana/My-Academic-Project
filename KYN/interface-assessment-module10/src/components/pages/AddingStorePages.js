import React from "react";
import AddingStore from "../content/AddingStore";
import Layout from "../layouts/Layout";

const AddingStorePages = () => {
  return (
    <Layout>
      <AddingStore />
    </Layout>
  );
};

export default AddingStorePages;
